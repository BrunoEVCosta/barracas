extends layout

block content
  a(href="/admin")
    h1 Gestão de accessos activos
  table.table.table-hover
    thead
      th ID
      th Nome
      th IP
      th Cidade
      th ISP
      th Plataforma
      th Revoke
    tbody
      for row in dados
        tr(accessId=row.accessId)
          td=row.accessId
          td=row.name 
          td=row.ip
          td=row.city
          td=row.isp
          td=row.platform
          td
            form(method="POST",action="/users/revoke/access/")
              input(id="id",name="id",type="hidden",value=row.accessId)
              button.btn.btn-dark(type="submit") Revoke